tool_id: "tslint:prefer-const"
extensions: ["js", "ts"]
tags: ["JavaScript", "TypeScript"]
default_weight: 200 # let eslint rules run first

command: 'echo "{\"jsRules\":{\"prefer-const\":true},\"rules\":{\"prefer-const\":true}}" > /c3pr/temp/tslint.json && tslint --fix -c /c3pr/temp/tslint.json "#{filename}"'

pr_title: Requires that variable declarations use const instead of let and var if possible
pr_body: |
  See: https://palantir.github.io/tslint/rules/prefer-const/
