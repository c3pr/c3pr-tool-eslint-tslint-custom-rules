multiple:
  - tool_id: "eslint:dot-location"
    extensions: ["js"]
    tags: ["JavaScript"]
    default_weight: 100
    command: 'eslint --fix "#{filename}" --no-eslintrc --env "es6" --env "node" --parser-options "{ecmaVersion: 2018}" --rule "{dot-location: \"error\"}"'

  - tool_id: "typescript-eslint:dot-location"
    extensions: ["js", "ts"]
    tags: ["JavaScript", "TypeScript"]
    default_weight: 300 # prefer eslint and tslint over this
    command: 'eslint --fix "#{filename}"  --parser "@typescript-eslint/parser" --plugin "@typescript-eslint" --rule "{dot-location: \"error\"}"'

pr_title: Consistent newlines before and after dots
pr_body: |
  See: https://eslint.org/docs/rules/dot-location
