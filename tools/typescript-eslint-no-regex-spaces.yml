tool_id: "typescript-eslint:no-regex-spaces"
extensions: ["js", "ts"]
tags: ["JavaScript", "TypeScript"]
default_weight: 300 # prefer eslint and tslint over this

command: 'eslint --fix "#{filename}"  --parser "@typescript-eslint/parser" --plugin "@typescript-eslint" --rule "{@typescript-eslint/no-regex-spaces: \"error\"}"'

pr_title: Remove multiple spaces in regular expressions
pr_body: |
  See: https://eslint.org/docs/rules/no-regex-spaces
